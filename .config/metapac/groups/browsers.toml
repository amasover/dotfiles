# metapac group: browsers — all three kept per Story 2.2 decision D4
# Membership source: docs/package-inventory.md; adopted in Story 2.8.
[arch]
packages = [
  "firefox",
  "google-chrome",
  "qutebrowser",
]
